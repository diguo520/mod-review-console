# 脚手架用 --dangerously-allow-all-builds 装依赖，批准列表不会落盘。
# pnpm 10+ 默认 verify-deps-before-run，裸 `pnpm build` 会再走 install 并报 ERR_PNPM_IGNORED_BUILDS。
# 自检应直接调 tsc/vite；这里关掉预检，避免模型仍敲 pnpm build 时卡死。
verify-deps-before-run=false

# 可选 install 脚本的放行清单在 pnpm-workspace.yaml 的 allowBuilds / onlyBuiltDependencies。
# 注意: 不要再写 `dangerously-allow-all-builds` —— pnpm 11 已不识别它(实测 `pnpm config get`
# 返回 undefined)，写了也等于没写；放行清单缺失时 CI 的 `pnpm install` 会以
# ERR_PNPM_IGNORED_BUILDS 退出 1，整个部署在安装阶段直接失败。